name: weather
description: Flutter Weather App
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  bloc: ^8.0.3
  dartz: ^0.10.1
  equatable: ^2.0.3
  firebase_core: ^1.15.0
  firebase_remote_config: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^5.0.2
  freezed_annotation: ^1.1.0
  get_it: ^7.2.0
  go_router: ^3.0.7
  injectable: ^1.5.2
  intl: ^0.17.0
  json_serializable: ^6.1.5
dev_dependencies:
  bloc_test: ^9.0.3
  build_runner: ^2.1.7
  fake_firebase_remote_config: ^1.0.2
  flutter_test:
    sdk: flutter
  freezed: ^1.1.0
  injectable_generator: ^1.5.2
  mocktail: ^0.2.0
  very_good_analysis: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
